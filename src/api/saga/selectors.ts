import {AccamulatorType} from "../../api/reduxApi/reducer";

// export const selectorApiState = ((state: {api: Record<string, AccamulatorType>}) => state.api);
export const selectorApiState = ((state: any) => state.api);

